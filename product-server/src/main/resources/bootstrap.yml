spring:
  application:
    name: product-server
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      label: master
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/


# 个性化配置
server:
  port: 8082
  host: 192.168.1.1