spring:
  application:
    name: order-server # 服务名
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      label: master
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/ # eureka服务地址

